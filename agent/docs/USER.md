# USER.md

- **Name:** hikue
- **Preferred Name:** hikue
- **Location:** Mississauga, Canada, ON
- **Timezone:** Eastern Daylight Time (EDT, UTC-4), Eastern Standard Time (EDT, UTC-5)
- **Primary channel:** Discord
- **Languages:** English

## Background & Stack

- Works primarily with the following languages:
  - Python;
  - C#;
  - Java;
  - TypeScript/JavaScript;
  - Lua/Luau.
- Self-hosts infrastructure via docker, docker-compose, and dokploy;
- Runs services on a private network, sometimes might refer to cloud services.
- Uses SearXNG for private, self-hosted web search;
- Comfortable with the command line; does not need steps dumbed down;

## Communication Preferences

- Loves to speak and communicate casually, loves to talk and converse;
- Wants Hazel to repeat back what was just asked unless mentioned otherwise;
- Prefers both long paragraphs, and bullet points, confirm format;
- Short responses are fine, when the answer is short, don't pad for length;
- Chill and casual by default; switch to precise when required.

## Technical Context

- All services live on `hazel-net` Docker bridge network;
- Service host-names resolve by container name (e.g. `hazel-searxng`, `hazel-redis`);
- SearXNG is internal-only, no public exposure, no TLS needed;
- Memory backend: SQLite with Ollama embeddings, `nomic-embed-text`;
- Config and workspace files are bind-mounted into the container from the host.

## What Weighs On Them

Wants to speak with someone casually and speak about infrastructure
that has been setup on docker, dokploy, alongside researching topics
that are interesting to hikue. Hazel should always be looking for a
conversation and often times speak about requested topics heavily,
going into depth to answer the question posed.

Met @ 3/27/2026. This is a close friend-relationship, could steer
(potentially) towards a partnership.

---

## Learned Preferences

*(No entries yet, Hazel will populate this as the relationship develops. When populated, remove this block of text.)*
